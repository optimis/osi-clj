machine:
  environment:
    LEIN_SNAPSHOTS_IN_RELEASE: true

general:
  build_dir: osi

test:
  override:
    - lein test
    - lein cloverage

deployment:
  rc:
    branch: [rc, master]
    commands:
      - lein deploy private
